import { ListPagination } from "./list-pagination";

describe("ListPagination", () => {
  it("exposes a component selector for list footers", () => {
    expect(ListPagination).toBeTruthy();
  });
});
