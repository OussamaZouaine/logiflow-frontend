name: Setup
description: Install pnpm 11, Node 24.20.0, and dependencies from the lockfile

runs:
  using: composite
  steps:
    - name: Configurer pnpm et Node.js
      uses: pnpm/setup@v2
      with:
        runtime: node@24.20.0
        cache: true
        require-lockfile: true
